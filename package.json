{
  "name": "your-project-name",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^9.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0",
    "netlify-lambda": "^2.0.16",
    "tailwindcss": "^3.0.0",
    "vite": "^4.0.0"
  },
  "scripts": {
      "dev": "vite",
      "build": "npm run vite-build && npm run functions-build",
      "preview": "vite preview",
      "start": "vite",
      "vite-build": "vite build",
      "functions-build": "netlify build --context production"
    },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
