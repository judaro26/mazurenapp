self.addEventListener('fetch', function(event) {
  // A service worker is required for PWA functionality.
  // This minimal fetch listener prevents the browser from
  // logging a fetch error when the service worker is active.
});
